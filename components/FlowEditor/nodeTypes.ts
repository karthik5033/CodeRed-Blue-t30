// Step 2 — Default Node Types for AvatarFlowX
// Using ReactFlow's built‑in nodes for now.
// Later (Chunk 3) we will create custom UIs.

import type { NodeTypes } from 'reactflow';


export const nodeTypes: NodeTypes = {
  // Using "input", "default", "output" built into ReactFlow
  // No custom components yet.
};

export default nodeTypes;
